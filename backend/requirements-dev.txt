# Development & test-only dependencies. Never installed in the runtime
# Docker image — see backend/Dockerfile, which installs only requirements.txt.
-r requirements.txt

pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.14.5
mypy==2.3.1
testcontainers[postgres]==4.15.0  # integration tests only, requires Docker
